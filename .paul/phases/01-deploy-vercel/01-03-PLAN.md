---
phase: 01-deploy-vercel
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: ["package.json", "package-lock.json", "components/Kasbon.tsx", ".gitignore"]
autonomous: true
---

<objective>
## Goal
Hapus dependency tidak terpakai (recharts), file dead code (Kasbon.tsx), dan tambah .env ke .gitignore untuk mencegah credential leak.
</objective>

<context>
@.paul/PROJECT.md
@package.json
@.gitignore
@components/Kasbon.tsx
</context>

<acceptance_criteria>
## AC-1: Codebase bersih dari dead code dan unused dependency
```gherkin
Given recharts tidak diimport di manapun, Kasbon.tsx tidak diimport di manapun, dan .env tidak ada di .gitignore
When plan ini dieksekusi
Then package.json tidak mengandung recharts, components/Kasbon.tsx tidak ada, dan .gitignore mengandung .env
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Hapus recharts, Kasbon.tsx, dan tambah .env ke .gitignore</name>
  <files>package.json, package-lock.json, components/Kasbon.tsx, .gitignore</files>
  <action>
    1. Uninstall recharts:
       npm uninstall recharts
       (menghapus dari package.json dan package-lock.json)

    2. Hapus components/Kasbon.tsx
       (confirmed tidak diimport di App.tsx maupun file lain)

    3. Tambah baris berikut ke .gitignore setelah baris "*.local":
       .env
       .env.*
       !.env.example

    JANGAN ubah file lain. JANGAN hapus recharts dari node_modules secara manual
    — biarkan npm uninstall yang handle.
  </action>
  <verify>
    1. cat package.json | grep recharts → harus kosong (tidak ditemukan)
    2. ls components/Kasbon.tsx → harus "No such file"
    3. grep "\.env" .gitignore → harus muncul .env dan .env.*
    4. npm run build → harus berhasil tanpa error
  </verify>
  <done>AC-1 satisfied: package.json bersih, Kasbon.tsx terhapus, .gitignore aman</done>
</task>
</tasks>

<output>
Setelah selesai, buat `.paul/phases/01-deploy-vercel/01-03-SUMMARY.md`
</output>
