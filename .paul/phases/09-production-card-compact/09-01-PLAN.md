---
phase: 09-production-card-compact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Production.tsx
autonomous: false
---

<objective>
## Goal
Compact mobile card produksi menjadi 2 kolom: kiri (nama baris-1, tanggal+komponen×qty baris-2), kanan (harga merge 2 baris) — dengan aksi edit/hapus muncul saat diklik.

## Purpose
Card produksi saat ini 3 baris dengan tombol aksi selalu tampil, memenuhi layar. Layout compact 2 kolom + toggle aksi membuatnya lebih ringkas dan konsisten dengan pola Components.tsx (Phase 8).

## Output
`components/Production.tsx`: mobile card section diganti layout flexbox 2 kolom + activeMenu toggle
</objective>

<context>
@.paul/PROJECT.md
@components/Production.tsx
</context>

<acceptance_criteria>

## AC-1: Layout 2 kolom pada mobile card produksi
```gherkin
Given halaman Produksi dibuka di viewport mobile (< md)
When daftar log produksi di-render
Then setiap card menampilkan:
  - Kiri baris 1: nama karyawan (bold, lebih besar)
  - Kiri baris 2: tanggal dd/mm + nama komponen x qty (kecil, muted)
  - Kanan (span 2 baris, center): harga total bold green
```

## AC-2: Aksi edit/hapus toggle saat diklik (ADMIN only)
```gherkin
Given user login sebagai ADMIN di viewport mobile
When card produksi diklik
Then tombol Edit dan Hapus muncul di bawah card
When card diklik lagi atau area luar diklik
Then tombol hilang
And user non-ADMIN tidak melihat tombol aksi
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Production mobile card — 2 kolom + toggle aksi</name>
  <files>components/Production.tsx</files>
  <action>
    **A. Tambah state activeMenu** setelah state `cleanupInputValue`:
    ```tsx
    const [activeMenu, setActiveMenu] = useState<string | null>(null);
    ```

    **B. Tambah useEffect close-on-outside-click** setelah useEffect `setCurrentPage`:
    ```tsx
    useEffect(() => {
      const handleClickOutside = (event: any) => {
        if (activeMenu && !event.target.closest('.prod-card')) {
          setActiveMenu(null);
        }
      };
      document.addEventListener('click', handleClickOutside);
      return () => document.removeEventListener('click', handleClickOutside);
    }, [activeMenu]);
    ```

    **C. Ganti seluruh blok "MOBILE COMPACT CARD VIEW"** (div md:hidden space-y-4, lines ~683-730):
    ```tsx
    {/* MOBILE COMPACT CARD VIEW */}
    <div className="md:hidden space-y-2">
      {currentItems.map(log => (
        <div
          key={log.id}
          className="prod-card bg-white dark:bg-dark-card rounded-xl shadow-sm border border-slate-200 dark:border-dark-border overflow-hidden"
        >
          <div
            className="flex items-stretch p-3 gap-3 cursor-pointer active:bg-slate-50 dark:active:bg-slate-800/50 transition-colors"
            onClick={() => setActiveMenu(activeMenu === log.id ? null : log.id)}
          >
            <div className="flex flex-col flex-1 min-w-0 gap-1">
              <span className="font-bold text-slate-800 dark:text-slate-100 text-sm truncate">
                {getEmpName(log.employeeId)}
              </span>
              <span className="text-[11px] text-slate-400 dark:text-slate-500 truncate">
                {log.date.slice(8,10)}/{log.date.slice(5,7)} · {getCompName(log.componentId)} ×{log.qty}
              </span>
            </div>
            <div className="flex items-center shrink-0">
              <span className="font-bold text-green-600 dark:text-green-400 text-sm whitespace-nowrap">
                Rp {log.total.toLocaleString('id-ID')}
              </span>
            </div>
          </div>

          {activeMenu === log.id && user?.role === 'ADMIN' && (
            <div className="flex gap-2 px-3 pb-3 border-t border-slate-100 dark:border-slate-800 pt-2">
              <button
                onClick={(e) => { e.stopPropagation(); handleOpen(log); setActiveMenu(null); }}
                className="flex-1 flex justify-center items-center gap-1 text-xs font-bold text-blue-600 bg-blue-50 dark:bg-blue-900/20 px-3 py-2 rounded-lg active:scale-95 transition-transform"
              >
                <Edit2 size={13} /> Edit
              </button>
              <button
                onClick={(e) => { e.stopPropagation(); setDeleteId(log.id); setActiveMenu(null); }}
                className="flex-1 flex justify-center items-center gap-1 text-xs font-bold text-red-600 bg-red-50 dark:bg-red-900/20 px-3 py-2 rounded-lg active:scale-95 transition-transform"
              >
                <Trash2 size={13} /> Hapus
              </button>
            </div>
          )}
        </div>
      ))}
    </div>
    ```

    Note: `Edit2`, `Trash2` sudah di-import. `Calendar` mungkin jadi unused — hapus dari import jika tidak dipakai di tempat lain dalam file.
  </action>
  <verify>
    npm run build — harus pass tanpa TypeScript error.
    Di browser mobile 375px: card compact 2 kolom, klik ADMIN → Edit+Hapus muncul.
  </verify>
  <done>AC-1 dan AC-2 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Production mobile card compact: 2 kolom + toggle aksi</what-built>
  <how-to-verify>
    1. Jalankan: npm run dev
    2. DevTools → iPhone SE (375px)
    3. Halaman Produksi — cek setiap card:
       - Kiri atas: nama karyawan (bold)
       - Kiri bawah: "13/06 · Nama Komponen ×5" (kecil, muted)
       - Kanan center: "Rp xxx" (bold green)
    4. Login ADMIN → klik card → Edit + Hapus muncul → klik lagi → hilang
    5. Desktop tabel tidak berubah
  </how-to-verify>
  <resume-signal>Ketik "approved" untuk lanjut ke UNIFY, atau deskripsikan masalah</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Desktop table view (div hidden md:block)
- Form modal input produksi
- Export modal dan logika PDF
- Pagination controls
- Cleanup modal

## SCOPE LIMITS
- Hanya ubah section mobile card (md:hidden) di Production.tsx
- Jangan tambah dependencies baru
- Jangan ubah logika filter, data loading, atau RBAC

</boundaries>

<verification>
- [ ] npm run build pass tanpa error
- [ ] Mobile card: 2 kolom (nama + detail kiri, harga kanan)
- [ ] Toggle aksi ADMIN berjalan
- [ ] Desktop view tidak berubah
- [ ] Import unused (Calendar) dihapus jika tidak dipakai lain
</verification>

<success_criteria>
- Build pass
- Visual verification approved
- Desktop tidak terpengaruh
</success_criteria>

<output>
After completion, create `.paul/phases/09-production-card-compact/09-01-SUMMARY.md`
</output>
