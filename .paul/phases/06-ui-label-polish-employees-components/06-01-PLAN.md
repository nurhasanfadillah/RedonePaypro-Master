---
phase: 06-ui-label-polish-employees-components
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Persingkat label button di halaman Daftar Karyawan dan Daftar Komponen menjadi satu kata (konsisten dengan Phase 4 decision).

## Purpose
Konsistensi label button di seluruh app — icon sudah menjelaskan fungsi, label ringkas hemat ruang di mobile.

## Output
- `components/Employees.tsx`: `Cleanup Data` → `Cleanup`, `Ekspor PDF` → `Ekspor`
- `components/Components.tsx`: `Cleanup Data` → `Cleanup`, `Ekspor PDF` → `Ekspor`
</objective>

<context>
@.paul/PROJECT.md
@components/Employees.tsx
@components/Components.tsx
</context>

<acceptance_criteria>
## AC-1: Employees buttons compact
Given user on Daftar Karyawan page
When page renders
Then buttons show: `Cleanup`, `Ekspor`, `Tambah` (single-word labels)

## AC-2: Components buttons compact
Given user on Daftar Komponen page
When page renders
Then buttons show: `Cleanup`, `Ekspor`, `Tambah` (single-word labels)
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Persingkat label button di Employees.tsx</name>
  <files>components/Employees.tsx</files>
  <action>
    Ganti label:
    - `Cleanup Data` → `Cleanup` (line 449)
    - `Ekspor PDF` → `Ekspor` (line 455)
    `Tambah` sudah benar, tidak diubah.
  </action>
  <verify>Grep "Cleanup Data" dan "Ekspor PDF" di Employees.tsx tidak ditemukan</verify>
  <done>AC-1 satisfied: Semua button Employees pakai label satu kata</done>
</task>

<task type="auto">
  <name>Persingkat label button di Components.tsx</name>
  <files>components/Components.tsx</files>
  <action>
    Ganti label:
    - `Cleanup Data` → `Cleanup` (line 361)
    - `Ekspor PDF` → `Ekspor` (line 368)
    `Tambah` sudah benar, tidak diubah.
  </action>
  <verify>Grep "Cleanup Data" dan "Ekspor PDF" di Components.tsx tidak ditemukan</verify>
  <done>AC-2 satisfied: Semua button Components pakai label satu kata</done>
</task>
</tasks>

<output>
After completion, create `.paul/phases/06-ui-label-polish-employees-components/06-01-SUMMARY.md`
</output>
