---
phase: 04-ui-label-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Production.tsx
  - components/Payments.tsx
  - components/RekapHasil.tsx
autonomous: true
---

<objective>
## Goal
Perpendek label teks button di header halaman Produksi, Pembayaran, dan Rekap agar lebih ringkas dan konsisten.

## Purpose
Label button saat ini terlalu panjang ("Ekspor PDF", "Cleanup Data", "Input Hasil", "Print Detail / PDF", dll.). Karena icon sudah cukup menjelaskan fungsinya, label cukup satu kata agar tampilan header lebih rapi di layar kecil.

## Output
Tiga file komponen diperbarui — label button di area header masing-masing halaman dipersingkat.
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Source Files
@components/Production.tsx
@components/Payments.tsx
@components/RekapHasil.tsx
</context>

<acceptance_criteria>

## AC-1: Button labels halaman Produksi dipersingkat
```gherkin
Given halaman Produksi terbuka
When user melihat tombol-tombol di header
Then tombol menampilkan: "Ekspor", "Cleanup", "Input" (bukan "Ekspor PDF", "Cleanup Data", "Input Hasil")
```

## AC-2: Button labels halaman Pembayaran dipersingkat
```gherkin
Given halaman Pembayaran terbuka
When user melihat tombol-tombol di header
Then tombol menampilkan: "Ekspor", "Cleanup", "Input" (bukan "Ekspor PDF", "Cleanup Data", "Input Transaksi")
```

## AC-3: Button labels halaman Rekap dipersingkat
```gherkin
Given halaman Rekap terbuka
When user melihat tombol-tombol di header
Then tombol menampilkan: "Periode", "Detail", "Global" (bukan "Ganti Periode", "Print Detail / PDF", "Print Global / PDF")
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Perbarui label button Production.tsx dan Payments.tsx</name>
  <files>components/Production.tsx, components/Payments.tsx</files>
  <action>
    Di Production.tsx:
    - Baris ~569: ubah teks "Ekspor PDF" → "Ekspor" (tetap setelah icon Printer)
    - Baris ~574: ubah teks "Cleanup Data" → "Cleanup" (tetap setelah icon Trash2)
    - Baris ~577: ubah teks "Input Hasil" → "Input" (tetap setelah icon Plus)

    Di Payments.tsx:
    - Baris ~450: ubah teks "Ekspor PDF" → "Ekspor" (tetap setelah icon Printer)
    - Baris ~455: ubah teks "Cleanup Data" → "Cleanup" (tetap setelah icon Trash2)
    - Baris ~461: ubah teks "Input Transaksi" → "Input" (tetap setelah icon Plus)

    Jangan ubah className, onClick, atau struktur JSX. Hanya ubah teks label.
  </action>
  <verify>Grep untuk teks lama: "Ekspor PDF", "Cleanup Data", "Input Hasil", "Input Transaksi" — tidak boleh ada di kedua file.</verify>
  <done>AC-1 dan AC-2 satisfied: label Production dan Payments telah dipersingkat.</done>
</task>

<task type="auto">
  <name>Task 2: Perbarui label button RekapHasil.tsx</name>
  <files>components/RekapHasil.tsx</files>
  <action>
    Di RekapHasil.tsx:
    - Baris ~416: ubah teks "Ganti Periode" → "Periode" (tetap setelah icon Filter)
    - Baris ~419: ubah teks "Print Detail / PDF" → "Detail" (tetap setelah icon FileText)
    - Baris ~422: ubah teks "Print Global / PDF" → "Global" (tetap setelah icon Printer)

    Jangan ubah className, onClick, atau struktur JSX. Hanya ubah teks label.
  </action>
  <verify>Grep untuk teks lama: "Ganti Periode", "Print Detail / PDF", "Print Global / PDF" — tidak boleh ada di file.</verify>
  <done>AC-3 satisfied: label RekapHasil telah dipersingkat.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Icon component (Printer, Trash2, Plus, Filter, FileText) — hanya ubah teks
- className styling pada button
- onClick handlers
- Logika kondisional `user?.role === 'ADMIN'`
- File lain di luar 3 file di atas

## SCOPE LIMITS
- Hanya ubah teks label button di area header (bukan label di modal, form, atau tempat lain)

</boundaries>

<verification>
Sebelum mendeklarasikan plan selesai:
- [ ] Grep "Ekspor PDF" di Production.tsx dan Payments.tsx → 0 hasil
- [ ] Grep "Cleanup Data" di Production.tsx dan Payments.tsx → 0 hasil
- [ ] Grep "Input Hasil" di Production.tsx → 0 hasil
- [ ] Grep "Input Transaksi" di Payments.tsx → 0 hasil
- [ ] Grep "Ganti Periode" di RekapHasil.tsx → 0 hasil
- [ ] Grep "Print Detail / PDF" di RekapHasil.tsx → 0 hasil
- [ ] Grep "Print Global / PDF" di RekapHasil.tsx → 0 hasil
- [ ] `npx tsc --noEmit` tidak ada error baru
</verification>

<success_criteria>
- Semua tasks completed
- Semua verification checks pass
- Label baru: Ekspor / Cleanup / Input (Production & Payments), Periode / Detail / Global (RekapHasil)
- Tidak ada perubahan fungsional — hanya teks label
</success_criteria>

<output>
After completion, create `.paul/phases/04-ui-label-polish/04-01-SUMMARY.md`
</output>
