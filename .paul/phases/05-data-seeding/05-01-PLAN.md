---
phase: 05-data-seeding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/seed.ts
autonomous: true
---

<objective>
## Goal
Seed NeonDB dengan 27 karyawan aktif dan 61 komponen dari dokumen perusahaan menggunakan script Drizzle sekali-jalan.

## Purpose
Database production kosong — data master harus diisi sebelum operator bisa mulai input produksi harian.

## Output
db/seed.ts yang dapat dijalankan ulang (idempotent via onConflictDoUpdate).
</objective>

<context>
@.paul/PROJECT.md
@db/schema.ts
@db/index.ts
</context>

<acceptance_criteria>

## AC-1: Karyawan ter-seed
```gherkin
Given db/seed.ts dijalankan
When script selesai tanpa error
Then tabel employees berisi 27 baris (KPRD-001 s/d KPRD-049)
```

## AC-2: Komponen ter-seed
```gherkin
Given db/seed.ts dijalankan
When script selesai tanpa error
Then tabel components berisi 61 baris (KP-001 s/d KP-061)
```

## AC-3: Idempoten
```gherkin
Given script sudah pernah dijalankan
When dijalankan lagi
Then tidak error (onConflictDoUpdate — upsert)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Buat db/seed.ts dengan data master lengkap</name>
  <files>db/seed.ts</files>
  <action>
    Buat script TypeScript self-contained:
    1. Import readFileSync dari 'fs' dan resolve dari 'path'
    2. Import drizzle dari 'drizzle-orm/neon-http', neon dari '@neondatabase/serverless'
    3. Import employees, components dari './schema'
    4. Fungsi loadEnv() — baca .env manual (tanpa dotenv), set process.env
    5. Panggil loadEnv() SEBELUM memanggil neon()
    6. Gunakan DATABASE_URL_UNPOOLED jika ada, fallback ke DATABASE_URL
    7. Buat db = drizzle(neon(connectionString))
    8. Array EMPLOYEES: 27 entri dari PDF (id, name, address)
    9. Array COMPONENTS: 61 entri dari PDF (id, name, price sebagai string)
    10. Fungsi async seed() yang:
        - Insert semua employees via onConflictDoUpdate
        - Insert semua components via onConflictDoUpdate
        - Log progress dan hasil
    11. Panggil seed().then(...).catch(...)

    Hindari: import db dari '../db/index' (karena sudah ada env loading sendiri)
  </action>
  <verify>npx tsc --noEmit (tidak ada TypeScript error)</verify>
  <done>AC-1, AC-2, AC-3: seed.ts ter-create dengan semua data</done>
</task>

<task type="auto">
  <name>Task 2: Jalankan seed script ke NeonDB production</name>
  <files>db/seed.ts (run only)</files>
  <action>
    Jalankan: npx tsx db/seed.ts
    Verifikasi output menunjukkan 27 employees dan 61 components berhasil di-insert.
  </action>
  <verify>Output log menampilkan "Seeded X employees" dan "Seeded Y components" tanpa error</verify>
  <done>AC-1 dan AC-2 satisfied: data live di NeonDB</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- db/schema.ts — schema tidak boleh diubah
- db/index.ts — connection shared tidak diubah
- api/* — endpoint tidak disentuh

## SCOPE LIMITS
- Seed hanya employees dan components
- Tidak membuat tabel baru
- Tidak memodifikasi data yang sudah ada (upsert aman)

</boundaries>

<verification>
- [ ] npx tsc --noEmit clean
- [ ] npx tsx db/seed.ts selesai tanpa error
- [ ] Output log: "Seeded 27 employees" dan "Seeded 61 components"
</verification>

<success_criteria>
- db/seed.ts ter-create
- Script berhasil dijalankan
- Data ter-insert ke NeonDB production
</success_criteria>

<output>
After completion, create `.paul/phases/05-data-seeding/05-01-SUMMARY.md`
</output>
