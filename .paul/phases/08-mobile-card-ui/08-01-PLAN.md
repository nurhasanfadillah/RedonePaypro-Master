---
phase: 08-mobile-card-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Employees.tsx
  - components/Components.tsx
autonomous: false
---

<objective>
## Goal
Perbaiki tampilan mobile card di halaman Karyawan (hapus alamat) dan halaman Komponen (grid 2 kolom + toggle aksi saat diklik).

## Purpose
Mobile card Karyawan menampilkan alamat yang tidak perlu — cukup nama + ID. Mobile card Komponen perlu layout 2 kolom yang lebih terstruktur (ID kiri-atas, nama kiri-bawah, harga kanan merge 2 baris) dan aksi edit/hapus hanya muncul saat diklik agar tidak memenuhi layar.

## Output
- `components/Employees.tsx`: Mobile card tanpa baris alamat/MapPin
- `components/Components.tsx`: Mobile card grid 2 kolom + `activeMenu` state untuk toggle aksi
</objective>

<context>
@.paul/PROJECT.md
@components/Employees.tsx
@components/Components.tsx
</context>

<acceptance_criteria>

## AC-1: Alamat dihapus dari mobile card Karyawan
```gherkin
Given halaman Karyawan dibuka di viewport mobile (< md breakpoint)
When daftar karyawan di-render
Then setiap card TIDAK menampilkan baris alamat/domisili
And nama karyawan, ID badge, dan shield icon (jika ada akses) tetap tampil
```

## AC-2: Layout grid 2 kolom pada mobile card Komponen
```gherkin
Given halaman Komponen dibuka di viewport mobile (< md breakpoint)
When daftar komponen di-render
Then setiap card menampilkan:
  - Kolom kiri baris 1: ID komponen (badge font-mono)
  - Kolom kiri baris 2: nama komponen (font-bold)
  - Kolom kanan (span 2 baris, center): harga bold purple
```

## AC-3: Aksi edit/hapus toggle saat card diklik (ADMIN only)
```gherkin
Given user login sebagai ADMIN di viewport mobile
When card komponen diklik
Then tombol Edit dan Hapus muncul di bawah card tersebut
When card diklik kembali atau card lain diklik
Then tombol hilang (toggle off)
And user non-ADMIN tidak melihat tombol aksi sama sekali
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Hapus baris alamat dari Employees mobile card</name>
  <files>components/Employees.tsx</files>
  <action>
    Di section "MOBILE CARD VIEW" (class `md:hidden`, sekitar baris 560-626):

    Hapus blok div berikut (baris 578-580):
    ```tsx
    <div className="flex items-center gap-1 text-[10px] text-slate-500 truncate">
       <MapPin size={10} /> {emp.address}
    </div>
    ```

    Setelah hapus, cek apakah `MapPin` masih digunakan di tempat lain dalam file.
    Jika tidak ada penggunaan lain, hapus `MapPin` dari baris import lucide-react di atas file.

    Tidak ada perubahan lain di Employees.tsx.
  </action>
  <verify>
    Buka DevTools → responsive 375px → halaman Karyawan → setiap card hanya tampilkan:
    - Avatar lingkaran dengan inisial nama
    - Nama karyawan (bold)
    - ID badge (font-mono)
    - Shield icon jika ada akses login
    — tanpa baris MapPin + alamat
  </verify>
  <done>AC-1 satisfied: alamat tidak tampil di mobile card karyawan</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Components mobile card — grid 2 kolom + toggle aksi</name>
  <files>components/Components.tsx</files>
  <action>
    **A. Tambah state activeMenu** setelah deklarasi state `cleanupInputValue` yang ada:
    ```tsx
    const [activeMenu, setActiveMenu] = useState<string | null>(null);
    ```

    **B. Tambah useEffect close-on-outside-click** setelah useEffect `setCurrentPage` yang ada:
    ```tsx
    useEffect(() => {
      const handleClickOutside = (event: any) => {
        if (activeMenu && !event.target.closest('.comp-card')) {
          setActiveMenu(null);
        }
      };
      document.addEventListener('click', handleClickOutside);
      return () => document.removeEventListener('click', handleClickOutside);
    }, [activeMenu]);
    ```

    **C. Ganti seluruh blok "MOBILE COMPACT LIST VIEW"** (dari `{/* MOBILE COMPACT LIST VIEW */}` sampai closing `</div>` pembungkus `space-y-2`) dengan:
    ```tsx
    {/* MOBILE COMPACT LIST VIEW */}
    <div className="md:hidden space-y-2">
      {currentItems.map(comp => (
        <div
          key={comp.id}
          className="comp-card bg-white dark:bg-dark-card rounded-xl shadow-sm border border-slate-200 dark:border-dark-border overflow-hidden"
        >
          <div
            className="grid p-3 gap-x-3 cursor-pointer active:bg-slate-50 dark:active:bg-slate-800/50 transition-colors"
            style={{ gridTemplateColumns: '1fr auto', gridTemplateRows: 'auto auto' }}
            onClick={() => setActiveMenu(activeMenu === comp.id ? null : comp.id)}
          >
            <span className="text-[10px] font-mono text-slate-400 bg-slate-100 dark:bg-slate-800 px-1.5 py-0.5 rounded w-fit">
              {comp.id}
            </span>
            <span
              className="font-bold text-purple-600 dark:text-purple-400 text-sm text-right self-center"
              style={{ gridRow: '1 / span 2' }}
            >
              Rp {comp.price.toLocaleString('id-ID')}
            </span>
            <h3 className="font-bold text-slate-800 dark:text-slate-100 text-sm truncate mt-0.5">
              {comp.name}
            </h3>
          </div>

          {activeMenu === comp.id && user?.role === 'ADMIN' && (
            <div className="flex gap-2 px-3 pb-3 border-t border-slate-100 dark:border-slate-800 pt-2">
              <button
                onClick={(e) => { e.stopPropagation(); handleOpen(comp); setActiveMenu(null); }}
                className="flex-1 flex justify-center items-center gap-1 text-xs font-bold text-blue-600 bg-blue-50 dark:bg-blue-900/20 px-3 py-2 rounded-lg active:scale-95 transition-transform"
              >
                <Edit2 size={13} /> Edit
              </button>
              <button
                onClick={(e) => { e.stopPropagation(); handleRequestDelete(comp); setActiveMenu(null); }}
                className="flex-1 flex justify-center items-center gap-1 text-xs font-bold text-red-600 bg-red-50 dark:bg-red-900/20 px-3 py-2 rounded-lg active:scale-95 transition-transform"
              >
                <Trash2 size={13} /> Hapus
              </button>
            </div>
          )}
        </div>
      ))}
    </div>
    ```

    Note: `Edit2` dan `Trash2` sudah di-import. Pastikan tidak ada import yang jadi unused setelah perubahan ini.
  </action>
  <verify>
    Di mobile 375px, halaman Komponen:
    - Setiap card: ID (kiri atas, badge mono), nama (kiri bawah, bold), harga (kanan, span 2 baris, purple bold)
    - Login ADMIN → klik card → Edit + Hapus muncul di bawah card → klik lagi → hilang
    - Login USER → klik card → tidak ada tombol aksi
    - Desktop table view tidak berubah
  </verify>
  <done>AC-2 dan AC-3 satisfied: grid 2 kolom benar, toggle aksi berjalan</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Mobile card Karyawan tanpa alamat + Components card dengan grid 2 kolom dan toggle aksi</what-built>
  <how-to-verify>
    1. Jalankan: `npm run dev`
    2. Buka http://localhost:5173, DevTools → Toggle device toolbar → iPhone SE (375px)
    3. **Halaman Karyawan:**
       - Card menampilkan: inisial avatar, nama, ID badge, shield (jika ada akses)
       - Tidak ada baris alamat/MapPin
    4. **Halaman Komponen:**
       - Card: ID kiri-atas, nama kiri-bawah, harga kanan (merge 2 baris)
       - Login ADMIN → klik card → tombol Edit dan Hapus muncul → klik lagi → hilang
       - Cek desktop (toggle off device toolbar) → tabel tidak berubah
  </how-to-verify>
  <resume-signal>Ketik "approved" untuk lanjut ke UNIFY, atau deskripsikan masalah untuk diperbaiki</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Desktop table view di Employees.tsx (div dengan class `hidden md:block`)
- Desktop table view di Components.tsx (div dengan class `hidden md:block`)
- Semua modal (form, access, cleanup, confirm) di kedua file
- Logika PDF export `handlePrint` di kedua file
- Pagination controls di Components.tsx
- Logika filter, sort, dan data loading

## SCOPE LIMITS
- Hanya ubah section mobile card (`md:hidden`) di kedua file
- Jangan tambah npm package baru
- Jangan ubah tipe data, API calls, atau logika bisnis
- Field `address` di Employee type tetap ada (dipakai form + PDF export)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build` berhasil tanpa TypeScript error
- [ ] `MapPin` dihapus dari import Employees.tsx (jika tidak digunakan lain)
- [ ] Mobile card Employees: tidak ada MapPin/address
- [ ] Mobile card Components: grid 2 kolom dengan harga span 2 baris
- [ ] Toggle aksi ADMIN di Components berjalan (muncul dan hilang)
- [ ] Desktop view kedua halaman tidak berubah
</verification>

<success_criteria>
- Semua tasks completed
- Build pass tanpa TypeScript error
- Visual verification approved via checkpoint
- Desktop view tidak terpengaruh
</success_criteria>

<output>
After completion, create `.paul/phases/08-mobile-card-ui/08-01-SUMMARY.md`
</output>
