---
phase: 02-migrate-neondb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - db/schema.ts
  - db/index.ts
  - drizzle.config.ts
  - .env.example
  - vercel.json
  - .paul/PROJECT.md
autonomous: false
---

<objective>
## Goal
Setup Drizzle ORM + NeonDB sebagai foundation layer: install deps, definisikan schema 5 tabel, konfigurasi drizzle-kit, perbaiki vercel.json, dan push schema ke NeonDB.

## Purpose
Tanpa foundation ini, Plan 02-02 (API Routes) tidak bisa dibuat — semua API route bergantung pada `db/index.ts` dan `db/schema.ts`.

## Output
- `db/schema.ts` — Drizzle table definitions (5 tabel)
- `db/index.ts` — Drizzle client instance
- `drizzle.config.ts` — konfigurasi drizzle-kit
- `package.json` — deps updated
- `vercel.json` — fix routing agar `/api/*` tidak ter-rewrite ke index.html
- `.env.example` — diupdate ke NeonDB variables
- Schema 5 tabel live di NeonDB
</objective>

<context>
@.paul/PROJECT.md
@.paul/phases/02-migrate-neondb/DISCOVERY.md
@package.json
@vercel.json
@.env.example
</context>

<acceptance_criteria>

## AC-1: Drizzle Schema Terdefinisi
```gherkin
Given file db/schema.ts ada
When dibaca
Then berisi 5 pgTable definitions: employees, components, production_logs, payments, app_users
  dengan kolom yang match dengan types.ts
```

## AC-2: Schema Live di NeonDB
```gherkin
Given DATABASE_URL_UNPOOLED ada di .env
When `npx drizzle-kit push` dijalankan
Then tidak ada error, 5 tabel terbentuk di NeonDB
```

## AC-3: Vercel Routing Benar
```gherkin
Given vercel.json yang baru
When request masuk ke /api/employees
Then tidak ter-rewrite ke index.html (API function terpanggil)
```

## AC-4: Supabase Dihapus
```gherkin
Given package.json yang baru
When dilihat dependencies
Then @supabase/supabase-js tidak ada, drizzle-orm dan @neondatabase/serverless ada
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Install deps + buat db/ layer + fix vercel.json + update .env.example</name>
  <files>
    package.json,
    db/schema.ts,
    db/index.ts,
    drizzle.config.ts,
    vercel.json,
    .env.example,
    .paul/PROJECT.md
  </files>
  <action>
    **1. Update package.json:**
    - Hapus dari dependencies: `@supabase/supabase-js`
    - Tambah ke dependencies: `drizzle-orm`, `@neondatabase/serverless`
    - Tambah ke devDependencies: `drizzle-kit`, `@vercel/node`
    - Jalankan: `npm install`

    **2. Buat db/schema.ts:**
    ```typescript
    import { pgTable, text, numeric, integer, timestamp } from 'drizzle-orm/pg-core';

    export const employees = pgTable('employees', {
      id: text('id').primaryKey(),
      name: text('name').notNull(),
      address: text('address'),
    });

    export const components = pgTable('components', {
      id: text('id').primaryKey(),
      name: text('name').notNull(),
      price: numeric('price', { precision: 15, scale: 2 }).notNull(),
    });

    export const productionLogs = pgTable('production_logs', {
      id: text('id').primaryKey(),
      date: text('date').notNull(),
      employeeId: text('employee_id').references(() => employees.id),
      componentId: text('component_id').references(() => components.id),
      qty: integer('qty').notNull(),
      priceSnapshot: numeric('price_snapshot', { precision: 15, scale: 2 }).notNull(),
      total: numeric('total', { precision: 15, scale: 2 }).notNull(),
      createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
    });

    export const payments = pgTable('payments', {
      id: text('id').primaryKey(),
      date: text('date').notNull(),
      employeeId: text('employee_id').references(() => employees.id),
      amount: numeric('amount', { precision: 15, scale: 2 }).notNull(),
      type: text('type').notNull(),
      note: text('note'),
      createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
    });

    export const appUsers = pgTable('app_users', {
      username: text('username').primaryKey(),
      password: text('password').notNull(),
      role: text('role').notNull(),
      employeeId: text('employee_id').references(() => employees.id),
      fullName: text('full_name'),
    });
    ```

    **3. Buat db/index.ts:**
    ```typescript
    import { drizzle } from 'drizzle-orm/neon-http';
    import { neon } from '@neondatabase/serverless';
    import * as schema from './schema';

    const sql = neon(process.env.DATABASE_URL!);
    export const db = drizzle(sql, { schema });
    ```

    **4. Buat drizzle.config.ts di root:**
    ```typescript
    import { defineConfig } from 'drizzle-kit';

    export default defineConfig({
      schema: './db/schema.ts',
      out: './drizzle',
      dialect: 'postgresql',
      dbCredentials: {
        url: process.env.DATABASE_URL_UNPOOLED!,
      },
    });
    ```

    **5. Fix vercel.json** — tambah rule eksplisit agar /api/* tidak ter-rewrite:
    ```json
    {
      "rewrites": [
        { "source": "/api/(.*)", "destination": "/api/$1" },
        { "source": "/(.*)", "destination": "/index.html" }
      ]
    }
    ```

    **6. Update .env.example** — ganti SUPABASE vars dengan NeonDB vars:
    ```
    # RedonePaypro — Environment Variables
    # Salin file ini menjadi .env dan isi nilainya.
    # Jangan commit file .env ke repository.

    # ─────────────────────────────────────────────
    # NEONDB (Required)
    # Dapatkan dari: https://console.neon.tech → Project → Connection Details
    # ─────────────────────────────────────────────

    # Pooled connection — digunakan app saat runtime (Vercel Functions)
    DATABASE_URL=postgresql://neondb_owner:***@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require

    # Direct connection — digunakan drizzle-kit push/pull (schema migrations)
    # Sama seperti DATABASE_URL tapi tanpa "-pooler" di hostname
    DATABASE_URL_UNPOOLED=postgresql://neondb_owner:***@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

    # ─────────────────────────────────────────────
    # GOOGLE GEMINI (Optional — belum aktif digunakan)
    # ─────────────────────────────────────────────
    GEMINI_API_KEY=your-gemini-api-key-here
    ```

    **7. Update .paul/PROJECT.md:**
    - Ubah "Application Type" section: ganti "Backend-as-a-Service: Supabase" → "Backend: Vercel Serverless Functions + NeonDB (PostgreSQL via Drizzle ORM)"
    - Ubah constraint: "Tidak ada custom backend — hanya Supabase" → "Backend: Vercel Functions di /api/ (server-side, credentials tidak di-expose ke browser)"
    - Update Tech Stack: ganti "Supabase JS SDK v2" → "Drizzle ORM + @neondatabase/serverless"
    - Update Environment Variables table: ganti VITE_SUPABASE_* → DATABASE_URL + DATABASE_URL_UNPOOLED

    Avoid: Jangan ubah informasi lain di PROJECT.md (value proposition, user roles, deployment info untuk v1.0).
  </action>
  <verify>
    - `npm install` selesai tanpa error
    - `node -e "require('./db/schema')"` tidak error (atau check dengan TypeScript)
    - `cat package.json` tidak mengandung @supabase/supabase-js
    - `cat vercel.json` mengandung dua rewrite rules
  </verify>
  <done>AC-1 dan AC-4 satisfied: db/schema.ts ada dengan 5 tabel, Supabase dihapus dari deps</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-to-do>
    Buat file `.env` di root project (jangan commit!) dengan isi:

    ```
    DATABASE_URL=postgresql://neondb_owner:PASSWORDMU@ep-small-voice-aoo9mk1c-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    DATABASE_URL_UNPOOLED=postgresql://neondb_owner:PASSWORDMU@ep-small-voice-aoo9mk1c.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    ```

    Catatan:
    - `DATABASE_URL` = connection string yang kamu berikan sebelumnya (pooler URL), tapi TANPA `channel_binding=require`
    - `DATABASE_URL_UNPOOLED` = sama tapi hapus `-pooler` dari hostname
    - Pastikan `.env` ada di `.gitignore`
  </what-to-do>
  <resume-signal>Ketik "done" setelah file .env dibuat</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Push schema ke NeonDB</name>
  <files>drizzle/ (generated)</files>
  <action>
    Jalankan drizzle-kit push untuk membuat 5 tabel di NeonDB:

    ```bash
    npx drizzle-kit push
    ```

    Jika ada prompt konfirmasi (Yes/No), jawab Yes.

    Jika error `channel_binding`: ini sudah dihindari dengan menghapusnya dari DATABASE_URL_UNPOOLED di .env.

    Jika error koneksi SSL: pastikan `?sslmode=require` ada di DATABASE_URL_UNPOOLED.
  </action>
  <verify>
    Output drizzle-kit push menunjukkan "Your schema is ready" atau berhasil membuat tabel.
    Verifikasi tambahan: `npx drizzle-kit studio` bisa dibuka dan menampilkan 5 tabel.
  </verify>
  <done>AC-2 satisfied: 5 tabel (employees, components, production_logs, payments, app_users) ada di NeonDB</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/` dan semua file frontend React — scope plan ini hanya setup backend layer
- `types.ts` — tipe sudah benar, jangan ubah
- `services/supabaseClient.ts` dan `services/dataService.ts` — akan diubah di Plan 02-02 dan 02-03
- `.gitignore` — jangan ubah (kecuali tambah `.env` jika belum ada di sana)

## SCOPE LIMITS
- Plan ini hanya setup foundation — tidak membuat API routes
- Tidak mengubah frontend components
- `drizzle-kit push` hanya di NeonDB fresh instance — tidak ada data migration

</boundaries>

<verification>
Sebelum menyatakan plan complete:
- [ ] `npm install` berhasil, tidak ada peer dependency errors
- [ ] `db/schema.ts` ada dengan 5 pgTable definitions
- [ ] `db/index.ts` ada, menggunakan neon() + drizzle()
- [ ] `drizzle.config.ts` ada dengan DATABASE_URL_UNPOOLED
- [ ] `package.json` tidak mengandung @supabase/supabase-js
- [ ] `vercel.json` mengandung 2 rewrite rules (api pass-through + SPA fallback)
- [ ] `.env.example` mengandung DATABASE_URL dan DATABASE_URL_UNPOOLED (tanpa VITE_ prefix)
- [ ] `npx drizzle-kit push` berhasil — 5 tabel ada di NeonDB
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Drizzle ORM terinstall dan schema terdefinisi
- Schema 5 tabel live di NeonDB
- vercel.json melindungi /api/* routes
- Supabase dependency dihapus
- PROJECT.md terupdate
</success_criteria>

<output>
Setelah completion, buat `.paul/phases/02-migrate-neondb/02-01-SUMMARY.md`
</output>
